name: 'Test Engine'
description: 'test engine for juicefs'
inputs:
  meta-url:  # id of input
    description: 'meta-url'
    required: true

runs:
  using: "composite"
  steps:
    - run: | 
        echo Meta-Url ${{ inputs.meta-url }}
        echo Meta-Url ${{ inputs.meta-url }}
      shell: bash
